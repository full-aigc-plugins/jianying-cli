[package]
name = "jianying-schema"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
jianying-domain = { path = "../jianying-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
