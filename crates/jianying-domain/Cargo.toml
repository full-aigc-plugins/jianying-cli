[package]
name = "jianying-domain"
version = "0.1.0"
edition = "2021"
description = "Unified, editor-independent domain model for jianying-cli"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
